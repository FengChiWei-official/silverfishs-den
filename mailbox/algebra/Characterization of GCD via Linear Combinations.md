---
tags:
  - type/permanent
  - attr/principle
  - topic/learning
  - status/in-progress
aliases:
  - theorem Z(a + b)
  - The Bézout Identity for Integers
  - Theorem 2.3.5
---

> [!tip]
> Property of  [[Subgroup Generation Theorem for the Integer Additive Group|Zd = Za + Zb]]
## Let

Let a, b be integers,  **not both 0**,  let $d := \gcd (a, b)$ be their [[Greatest Common Divisor]]. Consider the sub group of the addtive group, generated by a and b:
$$
\mathbf{S} = \mathbb{Z}a + \mathbb{Z}b
$$

>[!Note]
> Note: `GCD` is just a given name, its properties are listed following.
> Note: $\mathbf{S}$ is a subgroup for [[Subgroup Test]].

Then the following fundamental equality holds and provides a complete [[Characterization]] of $d$:
$$\mathbf{S} \equiv \mathbb{Z}d$$

From this equality, the defining properties of the [[Greatest Common Divisor|GCD]] are derived:
$$
	\begin{align}
	&\text{(a) }\text{d divides a and b} \\
	&\text{(b) }\text{if an integer e divides both a and b, it also divides d} \\
	&\text{(c) }\text{There are integers r and s such that} \quad d = ra + sb
	\end{align}
$$
>[!note]
>To be more specifically d is $\gcd (a, b)$.
>(a) proves `d` is a **common divisor** of `a` and `b`.
>(b) proves `d` is not less than any other **divisor**.
>(c) is The Bézout Identity for Integers, which provides a Linear [[Characterization]] of the [[Greatest Common Divisor|GCD]].

## Proof
[[Proof of Characterization of GCD via Linear Combinations]]


---

